<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Switchboard', root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to Federation.model_name.human(count: 2), federations_path %>
        </li>
        <li>
          <%= link_to Organisation.model_name.human(count: 2), organisations_path %>
        </li>
<% if current_user&.admin? or current_user&.is_an_operator? %>
        <li>
          <%= link_to User.model_name.human(count: 2), users_path %>
        </li>
<% end %>
      </ul>
      <ul class="nav navbar-nav pull-right">
<% if user_signed_in? -%>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.full_name %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to t(:profile), edit_user_registration_path  %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to t(:logout), destroy_user_session_path, method: :delete  %></li>
          </ul>
        </li>          
<% else -%>
        <li>
          <%= link_to t(:login), new_user_session_path%>
        </li>
<% end -%>
      </ul>
    </div>

  </div>
</nav>
